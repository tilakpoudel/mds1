
# Solution -> go back to stats nd use penalized 
# check missing values in R
colsum(is.na(data.frame))
sum(is.na(dataframe$columnname))

#Packages used to 
# mice(MICE) is the most used package that imputes the data automatically
# manually use na garne mice package use garere missing handling garne..

# amelia (alternative to mice) package


library(mice)
